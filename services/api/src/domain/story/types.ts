/**
 * Story Domain Types
 * Domain-specific types that are independent of providers
 */

// Re-export AI types for domain layer convenience
export type {
  StorySpec,
  EpisodeOutline,
  EpisodeText,
  PolicyProfile,
  SceneValidationResult
} from '../../ai/types';
